package com.ffl.study.scala.func

/**
  * @author lff
  * @datetime 2020/05/30 21:18
  *
  */
object WorldCount {

    def main(args: Array[String]): Unit = {
        //val data = List("hello world", "hello scala", "hello spark", "kafka")
        //
        //val tuples: immutable.Iterable[(String, Int, Int)] = data
        //  .flatMap(_.split(" "))
        //  //.map((_, 1))
        //  .groupBy(x => x)
        //  .map(x => (x._1, x._2.size, 3))
        //tuples

        //println(result)
    }
}
